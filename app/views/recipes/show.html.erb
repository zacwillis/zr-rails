<div class="container" style="margin-top: 40px;">
  <div class="row">
    <div class="col-md-6">
      <h2 style="margin-bottom: 0px;"><%= @recipe[:name] %></h2>

      <hr style="margin-top: 8px;">

      <h4>Ingredients</h4>

      <% @recipe[:ingredients].each do |ing| %>
        <li style="text-indent: 20px;"><%= ing.name %> - <%= ing.quantity %></li>
      <% end %>

      <hr>

      <h4>Instructions</h4>

      <% @recipe[:instructions].each do |inst| %>
        <p style="text-indent: 20px;"><%= inst.step %>.&nbsp;&nbsp; <%= inst.description %></p>
      <% end %>
    </div>
    <div class="col-md-6">
      <%= image_tag @recipe[:image_url], width: '100%', height: '100%' %>
      <div style="margin-top: 10px;">
        <%= button_to "Back To Recipes", recipes_path, method: :get, class: "btn btn-dark btn-sm" %>
      </div>
    </div>
  </div>
  <!-- Add edit info -->
</div>